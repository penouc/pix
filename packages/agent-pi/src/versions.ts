/**
 * Locked Pi stack versions (plan §5 / §7.2).
 * Bump only with contract tests + fixed evaluation suite.
 */
export const PI_SDK_PACKAGES = {
  codingAgent: '@earendil-works/pi-coding-agent',
  agentCore: '@earendil-works/pi-agent-core',
  ai: '@earendil-works/pi-ai',
  version: '0.83.0',
} as const;
