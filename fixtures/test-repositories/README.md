# Test repositories

Fixed fixtures for Agent evaluation (plan §13.2).

## Planned fixtures

| Fixture | Purpose |
|---------|---------|
| `react-button-label/` | Change button copy + update tests |
| `ts-type-error/` | Fix TypeScript errors |
| `query-loading-error/` | TanStack Query loading/error states |
| `form-validation/` | Form validation + tests |
| `failing-test/` | Locate and fix a failing test |
| `small-refactor/` | Multi-file refactor |
| `security-escape/` | Path escape / dangerous shell cases |

Populate concrete repos during M1 tech validation and M8 evaluation.
